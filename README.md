# NutriSense AI

> Pakistani and South Asian food recognition system — photograph a dish, get instant nutrition and a plain-language insight. Built around a fine-tuned EfficientNetB0 CNN.

<p align="left">
  <img alt="Python"      src="https://img.shields.io/badge/python-3.12-blue">
  <img alt="React"       src="https://img.shields.io/badge/react-19-61dafb">
  <img alt="React Native" src="https://img.shields.io/badge/react%20native-0.81-61dafb">
  <img alt="Flask"       src="https://img.shields.io/badge/flask-3.0-black">
  <img alt="ONNX"        src="https://img.shields.io/badge/runtime-ONNX-lightgrey">
  <img alt="License"     src="https://img.shields.io/badge/license-academic-green">
</p>

---

## Why this exists

Mainstream nutrition apps (MyFitnessPal, Cronometer, Noom) are built around Western food databases. Pakistani users get missing entries, wrong calorie estimates, or outright recognition failures for everyday dishes like **karahi**, **halwa puri**, **nihari**, or **paya**.

This is a documented research gap:
- **Scientific Reports (2025)** — no quality dataset for South Asian cuisine exists in the mainstream food-recognition literature
- **Tahir et al. (2020)** — the first Pakistani food dataset has only ~49 images per class, too few for reliable CNN fine-tuning
- **JMIR (2024)** — 70 % of users abandon health apps within 100 days, driven primarily by manual food entry

NutriSense AI fills the gap with a fine-tuned EfficientNetB0 trained on a curated multi-source South Asian dataset and shipped end-to-end with a mobile app, a web dashboard, and a Flask inference API.

## What it does

1. **Snap** — point your phone (or upload on web) at any South Asian dish
2. **Identify** — the CNN classifies among ~100+ food classes (~35 South Asian)
3. **Understand** — get calories / protein / carbs / fat per standard serving plus a 2-3 sentence, goal-personalised insight generated by an LLM

No manual entry. No calorie counting. No meal plans. Just snap → understand.

## Architecture

```
┌──────────────────┐     ┌────────────────────┐
│  React Native    │     │  React + Vite      │
│  (Expo Router)   │     │  + Tailwind        │
│  mobile app      │     │  web dashboard     │
└────────┬─────────┘     └──────────┬─────────┘
         │                          │
         └──────────┬───────────────┘
                    │  multipart/form-data
                    ▼
         ┌──────────────────────────────────┐
         │   Flask API   (hosted on Render) │
         │   ── predict.py   (ONNX inference)│
         │   ── nutrition.py (local JSON DB) │
         │   ── insights.py  (OpenRouter API)│
         │   ── gradcam_api.py (static URLs) │
         └──────────────────────────────────┘
                    │
                    ▼
         ┌──────────────────────────────────┐
         │  Supabase                         │
         │  ── PostgreSQL (auth + scans)    │
         │  ── Storage (images + Grad-CAM)  │
         └──────────────────────────────────┘
```

## Tech stack

| Layer | Choice | Why |
|---|---|---|
| Model | **EfficientNetB0** (5.3 M params) | Best accuracy/size ratio; fits Render's 512 MB free tier as ONNX |
| Training | PyTorch on Kaggle T4 x2 | Free GPU; reproducible notebook |
| Inference | **ONNX Runtime** (CPU) | 20-40 % faster than PyTorch single-image; tiny memory footprint |
| Backend | Flask + gunicorn | Minimal overhead; three endpoints |
| Database / Auth / Storage | **Supabase** | All-in-one free tier with PostgreSQL + RLS + Storage |
| LLM insights | **OpenRouter** (`qwen/qwen-2.5-72b-instruct`) | OpenAI-compatible API; free tier |
| Mobile | React Native + Expo Router | iOS + Android with file-based routing |
| Web | React 19 + Vite 5 + Tailwind 4 | Fast dev loop; standard tooling |
| Hosting | Render (backend) · Vercel (web) · EAS (Android APK) | All free tiers |

## Academic contribution

The primary contribution is the **fine-tuned EfficientNetB0 trained on a curated multi-source South Asian food dataset**. The dataset combines:

| Source | Classes | Images / class | Role |
|---|---|---|---|
| Food-101 | 101 | ~1,000 | Western baseline |
| Khana 2025 (Indian Food Images) | 80+ | ~1,600+ | South Asian backbone |
| DeshiFoodBD | 19 | ~285 | Bangladeshi overlap |
| Self-scraped Pakistani | 13-20 | 300-500 | Gap-fill for dishes absent from existing datasets |

An ablation study compares EfficientNetB0 against MobileNetV2 and ResNet50 on the same train/val split with identical hyperparameters to justify the architecture choice. Grad-CAM heatmaps visualise the model's attention regions for South Asian dishes.

## Repository layout

```
backend/      Flask API (Python)
├── app.py           Flask entry point + CORS
├── config.py        Env-var loading
├── predict.py       ONNX inference
├── nutrition.py     Local JSON nutrition lookup
├── insights.py      OpenRouter (Qwen) call
├── gradcam_api.py   Precomputed Grad-CAM URL lookup
├── routes/          Blueprint per endpoint
├── requirements.txt
├── Procfile
└── test_api.py      Integration tests

mobile/       React Native + Expo Router
├── app/             File-based routes (auth, onboarding, tabs)
├── components/      Shared UI
├── lib/             Supabase + API client
└── package.json

web/          React + Vite + Tailwind
├── src/pages/       Landing, Login, Dashboard, History, Insights, Chatbot, Profile
├── src/components/  Navbar, ProtectedRoute, ResultCard, UploadZone, charts
├── src/context/     AuthContext
├── src/lib/         Supabase + API client
└── package.json

model/        Training scripts (PyTorch — Kaggle only)
├── dataset.py       PyTorch Dataset + augmentation + class weights
├── model.py         EfficientNetB0 + freeze/unfreeze helpers
├── train.py         Two-phase transfer learning
├── ablation.py      EfficientNetB0 vs MobileNetV2 vs ResNet50
├── evaluate.py      Held-out val accuracy + confusion matrix + per-class chart
├── gradcam.py       Grad-CAM heatmap generation
├── export.py        PyTorch → ONNX
├── scrape.py        icrawler-based scraper for gap-fill classes
├── curate_classes.py  Dataset merger + class normaliser
├── requirements.txt
└── nutrisense_training.ipynb  Kaggle orchestration notebook

data/         Static data files (in repo)
├── class_index.json    Generated by curate_classes.py
├── nutrition_db.json   Manually curated from USDA + Pakistani tables
└── gradcam_index.json  Populated after Kaggle Grad-CAM upload

supabase/
└── schema.sql       Tables + RLS policies + auto-trigger
```

## API contract

### POST /predict

```
Request: multipart/form-data
  image      File    JPEG or PNG, compressed to <500 KB by the client
  user_goal  string  'weight_loss' | 'muscle_gain' | 'curious'

Response 200:
{
  "top_prediction": { "label": "chicken_karahi", "confidence": 0.91 },
  "top_3": [
    { "label": "chicken_karahi", "confidence": 0.91 },
    { "label": "chicken_handi",  "confidence": 0.06 },
    { "label": "butter_chicken", "confidence": 0.03 }
  ],
  "low_confidence": false,
  "nutrition": { "calories": 320, "protein": 28, "carbs": 8, "fat": 19 },
  "insight": "Karahi is a high-protein dish — well suited to your muscle-gain goal...",
  "gradcam_sample_url": "https://...supabase.co/storage/.../chicken_karahi_gradcam.png",
  "processing_time_ms": 1240
}
```

`low_confidence: true` when `top_prediction.confidence < 0.70`. Frontend then shows a top-3 disambiguation picker.

### GET /health
```json
{ "status": "ok", "model_loaded": true, "classes": 100 }
```

### GET /classes
```json
{ "0": "aloo_paratha", "1": "anda_paratha", ... }
```

## Running locally

### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate          # Windows: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env              # fill in OPENROUTER_API_KEY + SUPABASE_*
python app.py                     # http://localhost:5000
python test_api.py                # run the 5 integration tests
```

Set `MOCK_MODE=true` in `.env` to run without a trained `model.onnx` — useful for frontend development before training completes.

### Web

```bash
cd web
npm install
cp .env.example .env              # fill in VITE_SUPABASE_*, VITE_API_BASE_URL, VITE_OPENROUTER_KEY
npm run dev                       # http://localhost:5173
```

### Mobile

```bash
cd mobile
npm install --legacy-peer-deps
cp .env.example .env              # set EXPO_PUBLIC_API_BASE_URL to your machine's LAN IP
npx expo start                    # scan QR with Expo Go on your phone
```

### Supabase

1. Create a free project at [supabase.com](https://supabase.com)
2. Paste [`supabase/schema.sql`](supabase/schema.sql) into the SQL Editor and run
3. Create two storage buckets: `scan-images` (public) and `gradcam` (public)
4. Copy the project URL, anon key, and service-role key into the `.env` files

### Training (optional)

The model is trained on Kaggle GPU. See [`model/nutrisense_training.ipynb`](model/nutrisense_training.ipynb). Runtime is roughly 6-8 hours on T4 x2 for the full pipeline (curate → train → ablate → evaluate → export).

## Deployment

- **Backend** → Render (configured in [`render.yaml`](render.yaml))
- **Web** → Vercel (root directory: `web`, framework: Vite)
- **Mobile** → EAS build (`eas build -p android --profile preview`)

## License

This repository is published for academic and educational reference. The model weights, datasets, and final report are subject to separate terms.

## Acknowledgements

- **Food-101** dataset (Bossard et al., ETH Zürich)
- **Khana 2025** Indian food dataset (arXiv:2509.06006)
- **DeshiFoodBD** Bangladeshi food dataset (Mendeley Data)
- **Tahir et al. (2020)** Pakistani Food Dataset
- **EfficientNet** (Tan & Le, ICML 2019)
- **Supabase**, **Render**, **Vercel**, **OpenRouter**, **Kaggle** — free-tier infrastructure
